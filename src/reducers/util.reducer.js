import { ActionTypes } from '../constants';
import React from "react";

const initialStateUtil = {
    fileStorage: "/storage/files"
};

const utilReducer = (state = initialStateUtil, action) => {

};

export default utilReducer;
